---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: nifiregistries.registry.nifi.oper
spec:
  group: registry.nifi.oper
  names:
    kind: NifiRegistry
    listKind: NifiRegistryList
    plural: nifiregistries
    singular: nifiregistry
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: NifiRegistry is the Schema for the nifiregistries API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NifiRegistrySpec defines the desired state of NifiRegistry
            properties:
              database:
                description: Database defines the external database connection settings
                properties:
                  driverClass:
                    type: string
                  driverDownloadURL:
                    description: DriverDownloadURL is the URL from which the JDBC
                      driver can be downloaded by an InitContainer.
                    type: string
                  enabled:
                    type: boolean
                  password:
                    type: string
                  secretName:
                    type: string
                  url:
                    type: string
                  username:
                    type: string
                type: object
              flowStorage:
                description: FlowStorage defines the settings for Nifi Registry flow
                  persistence
                properties:
                  enabled:
                    type: boolean
                  size:
                    type: string
                  storageClass:
                    type: string
                type: object
              image:
                description: Image defines the Nifi Registry container image to use
                properties:
                  repository:
                    type: string
                  tag:
                    type: string
                type: object
              initImage:
                description: InitImage is the Docker image used for all InitContainers
                  (e.g., busybox or another base image).
                type: string
              keycloak:
                description: Keycloak defines the OIDC settings for Keycloak authentication
                properties:
                  claimIdentifyingUser:
                    default: preferred_username
                    description: ClaimIdentifyingUser is the claim in the ID token
                      used to identify the user (e.g., preferred_username).
                    type: string
                  clientId:
                    type: string
                  clientSecretName:
                    description: ClientSecretName is the name of the Kubernetes Secret
                      containing the client secret.
                    type: string
                  discoveryUrl:
                    description: DiscoveryUrl is the base URL for the OIDC provider's
                      discovery endpoint.
                    type: string
                  enabled:
                    default: false
                    description: Enabled indicates whether Keycloak OIDC authentication
                      is enabled.
                    type: boolean
                  externalUrl:
                    description: ExternalURL is the publicly accessible root URL for
                      the NiFi Registry (e.g., https://registry.k8c.ru).
                    type: string
                  initialAdminIdentity:
                    description: InitialAdminIdentity is the username from Keycloak
                      that will be granted initial admin privileges (e.g., root).
                    type: string
                  realm:
                    description: Realm is the name of the Keycloak realm to connect
                      to (e.g., nifier).
                    type: string
                type: object
              libStorage:
                description: LibStorage defines the settings for NiFi Registry library
                  files PVC.
                properties:
                  enabled:
                    description: Enabled указывает, включен ли PVC для каталога lib.
                    type: boolean
                  size:
                    description: Size определяет размер PVC (например, 1Gi, 200Mi).
                    type: string
                  storageClass:
                    description: StorageClass определяет имя StorageClass для PVC.
                    type: string
                type: object
              postgreSQL:
                description: PostgreSQL defines the settings for the managed PostgreSQL
                  instance
                properties:
                  database:
                    type: string
                  enabled:
                    type: boolean
                  image:
                    description: ImageSpec defines the container image repository
                      and tag
                    properties:
                      repository:
                        type: string
                      tag:
                        type: string
                    type: object
                  password:
                    type: string
                  size:
                    type: string
                  storageClass:
                    type: string
                  username:
                    type: string
                type: object
              resources:
                description: Resources defines the compute resources for the Nifi
                  Registry Pod
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.


                      This is an alpha field and requires enabling the
                      DynamicResourceAllocation feature gate.


                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              size:
                default: 1
                format: int32
                maximum: 3
                minimum: 1
                type: integer
              tls:
                description: Tls defines the TLS settings for the Nifi Registry
                properties:
                  adminIdentity:
                    type: string
                  clientAuth:
                    default: REQUIRED
                    description: ClientAuth determines if mutual TLS is required (REQUIRED
                      or WANTED)
                    type: string
                  enabled:
                    type: boolean
                  host:
                    type: string
                  keystorePassword:
                    type: string
                  port:
                    format: int32
                    type: integer
                  truststorePassword:
                    type: string
                type: object
            type: object
          status:
            description: NifiRegistryStatus defines the observed state of NifiRegistry
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
